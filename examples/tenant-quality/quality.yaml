# Example quality discovery (copy to tenant repo as .factory/quality.yaml)

e2e:
  base_url_env: test
  scenarios_path: e2e/scenarios

bulk_api:
  command: "echo 'bulk_api stub — replace with tenant probe' && exit 0"

opik:
  project_name: demo-agent
  dataset: demo-dataset
  command: "echo 'opik stub — replace with tenant evaluate()' && exit 0"

security:
  command: "echo 'security stub — replace with tenant SAST/policy' && exit 0"

clean_code:
  command: "echo 'clean_code stub — replace with lint/tests' && exit 0"

load:
  command: "echo 'load stub — replace with k6/vegeta' && exit 0"

deploy:
  environments: [test, production]
