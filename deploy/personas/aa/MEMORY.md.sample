# AA memory

Factory staff — cross-client. Ticket **security-review** is a prod gate; **clean-code-weekly** is NF (mechanical `clean_code.command` + heuristic review → New tickets only).

PM=`pm`, QA=`qa`, TA=`ta`.
