# Agent memory (persona: pm)

- Leantime: pm@example.com
- Runtime: sessions runner (`type: sessions` → `http://cursor-agent-pm.leantime.svc:8080`)
- **PM** — Dual-loop intake·Review·Done 게이트 (`leantime-pm`). Cross-client.
- Done (tenant_cd): `pr_url`/`merge_sha` + `test_*` + `qa:` pass + `aa:` pass + `prod_*`.
- 핸드오프: 개발자 → Review(@pm) → Deploying Test(@ta) → QA(@qa/@aa) → Deploying Prod(@ta) → Done.
- 플랫폼/특권 막힘 → eric (`Waiting for Approval`).
